module.exports = {
  // 配置 dependency
  dependency: {
    platforms: {
      ios: {
        automaticPodsInstallation: true,
      },
    },
  },
  
  // 配置 dependencies
  dependencies: {
    'react-native-vector-icons': {
      platforms: {
        ios: null,
      },
    },
  },
};
